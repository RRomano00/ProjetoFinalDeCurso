/** Coordenadas padrão do mapa — Santa Rita do Sapucaí, MG (item 4: todos os mapas iniciam aqui). */
export const SANTA_RITA_DO_SAPUCAI = { lat: -22.2522, lng: -45.7036 };

/** Zoom inicial padrão dos mapas. */
export const DEFAULT_MAP_ZOOM = 14;
